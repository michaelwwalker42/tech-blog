<div>
  {{#each this}}
  <div class="card">
    <div class="card-header">
      {{user.username}} on {{format_date created_at}}
    </div>
    <div>
      {{comment_text}}
    </div>
  </div>
  {{/each}}
</div>